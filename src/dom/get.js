//>>excludeStart("exclude", pragmas.exclude);
define([ "shoestring" ], function(){
//>>excludeEnd("exclude");

	shoestring.fn.get = function( num ){
		return this[ num ];
	};

//>>excludeStart("exclude", pragmas.exclude);
});
//>>excludeEnd("exclude");
