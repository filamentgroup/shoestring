//>>excludeStart("exclude", pragmas.exclude);
define([ "shoestring" ], function(){
//>>excludeEnd("exclude");

  shoestring.cssExceptions = {
		'float': [ 'cssFloat' ]
	};

//>>excludeStart("exclude", pragmas.exclude);
});
//>>excludeEnd("exclude");
